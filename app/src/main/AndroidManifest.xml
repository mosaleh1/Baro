<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.runcode.baro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Activities.AddLessonActivity"/>
        <activity android:name=".ui.Activities.LessonInfoActivity" />
        <activity android:name=".ui.Activities.CourseLessonsActivity" />
        <activity android:name=".ui.Activities.AddCourseActivity" />
        <activity android:name=".ui.Activities.RegisterActivity" />
        <activity android:name=".ui.Activities.LoginActivity" />
        <activity android:name=".ui.Activities.MainActivity" />
        <activity android:name=".ui.Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>